import { OptionTypeIconPipe } from './option-type-icon.pipe';

describe('OptionTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
