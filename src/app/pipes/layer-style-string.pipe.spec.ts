import { LayerStyleStringPipe } from './layer-style-string.pipe';

describe('LayerStyleStringPipe', () => {
  it('create an instance', () => {
    const pipe = new LayerStyleStringPipe();
    expect(pipe).toBeTruthy();
  });
});
