import { AnimationPausePipe } from './animation-pause.pipe';

describe('AnimationPausePipe', () => {
  it('create an instance', () => {
    const pipe = new AnimationPausePipe();
    expect(pipe).toBeTruthy();
  });
});
