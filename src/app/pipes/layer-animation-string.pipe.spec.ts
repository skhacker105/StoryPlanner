import { LayerAnimationStringPipe } from './layer-animation-string.pipe';

describe('LayerAnimationStringPipe', () => {
  it('create an instance', () => {
    const pipe = new LayerAnimationStringPipe();
    expect(pipe).toBeTruthy();
  });
});
