import { SpacePaddedPipe } from './space-padded.pipe';

describe('SpacePaddedPipe', () => {
  it('create an instance', () => {
    const pipe = new SpacePaddedPipe();
    expect(pipe).toBeTruthy();
  });
});
