import { DescriptionToolTipDirective } from './description-tool-tip.directive';

describe('DescriptionToolTipDirective', () => {
  it('should create an instance', () => {
    const directive = new DescriptionToolTipDirective();
    expect(directive).toBeTruthy();
  });
});
