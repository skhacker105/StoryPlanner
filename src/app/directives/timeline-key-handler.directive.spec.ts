import { TimelineKeyHandlerDirective } from './timeline-key-handler.directive';

describe('TimelineKeyHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineKeyHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
